version: 2

sources:
 - name: datafeed_shared_schema
   database: "{{('demo_sales')}}" 
   schema: raw_schema
   tables:
     - name: raw_customers
     - name: raw_orders
     - name: customers